# OCaml Todo CLI

Tasks are saved to `~/.todo.sexp`. Open it — it's human-readable:

```sexp
((id 1) (title "Buy milk") (status Todo) (priority Medium))
((id 2) (title "Fix bug") (status Done) (priority High))
```
